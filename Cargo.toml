[package]
name = "invoice-generator"
version = "0.1.0"
edition = "2024"

[dependencies]
inquire = "0.7"
serde = { version = "1", features = ["derive"] }
serde_yaml = { package = "yaml_serde", version = "0.10" }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
